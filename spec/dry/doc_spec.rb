require "spec_helper"

RSpec.describe Dry::Doc do
  it "has a version number" do
    expect(Dry::Doc::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
